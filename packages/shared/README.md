# @stim-io/shared

Shared support package for `stim-packages`.

This package holds reusable support for the package workspace below product/business composition.

- `.` exports browser/runtime primitives that do not depend on `@stim-io/components`.
- `./node` exports package lifecycle tooling helpers for workspace package scripts.
